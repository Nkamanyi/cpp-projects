#include <iostream>
#include <cmath>

using namespace std;

int main(){
    double x = 3.14;
    double y = 4;
    double z;

    //z = max(x, y);
    //z = min(x, y);
    //z = pow(2, 4);
    //z = sqrt(pow(x, 2) + pow(y, 2));
    //z = abs(x);
    //z = round(x);
    //z = ceil(x);
    //z = floor(x);
    //z = trunc(x);
    //z = round(x);

    cout << "The min of x and y is: " << z << endl;

    return 0;
}